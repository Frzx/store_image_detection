[project]
name = "object-detection-tesco-frontend"
version = "0.1.0"
description = "Frontend for the object detection app"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.135.3",
    "httpx>=0.28.1",
    "jinja2>=3.1.6",
    "pydantic-settings>=2.11.0",
]
